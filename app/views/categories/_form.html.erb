<!-- Category form -->
<!-- Basic form fields -->
<!-- Not public front end so doesn't have to be production standard -->
<%= simple_form_for @category do |f|%>
  <%= f.input :name, label: "Category Name" %>
  <%= f.file_field :category_img %>
  <%= f.button :submit %>
<% end %>